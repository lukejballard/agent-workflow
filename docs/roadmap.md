# Project Roadmap

This roadmap needs a product-specific refresh for **baseball-biomechanics-ai**.

The previous content in this file described legacy platform ideas that do not map cleanly to the current baseball biomechanics application, so it has been removed rather than preserved as misleading guidance.

Until maintainers publish a validated roadmap, use these sources for planning context:

1. `docs/specs/active/` for current in-flight work.
2. `openspec/changes/` for brownfield change proposals.
3. `plan/` for local planning artifacts.

Do not treat older roadmap copies or inherited planning notes as current product direction unless they are explicitly revalidated.
